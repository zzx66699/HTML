# Flex
## flex-wrap
```css
.thumbnails {
    display: flex;
    /* wrap child elements with a max width instead of just laying them out horizontally. */
    flex-wrap: wrap;
    max-width: 560px;
    margin: 50px auto;
}
```